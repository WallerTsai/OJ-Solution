# A - 消失的数字 (CSES - 1083)

给你从 $1, 2, \dots, n$ 的所有数字，唯独少了一个。你的任务就是找出这个缺失的数字。

## 输入

第一行输入一个整数 $n$。
第二行输入 $n - 1$ 个数字。每个数字都不相同，且范围在 $1$ 到 $n$（含）之间。

## 输出

输出那个缺失的数字。

## 约束

* $2 \le n \le 2 \cdot 10^5$

## 示例

```text
Input:
5
2 3 1 5

Output:
4
```